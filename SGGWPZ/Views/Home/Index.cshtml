@model ViewPlanZajec

@{
    ViewData["Title"] = "Home Page";
}

@Html.Partial("/Views/Home/PlanZajecNavBar.cshtml", Model)
@if (Model.grupa == "A" || Model.grupa == "B")
{
    @Html.Partial("/Views/Home/PlanZajecN.cshtml", Model)
}
else
{
    @Html.Partial("/Views/Home/PlanZajec.cshtml", Model)
}
